<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".BarcodeScan">

    <SurfaceView
        android:id="@+id/SurfaceView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY"
        android:layout_below="@id/Preview"
        android:layout_above="@id/imageView"/>

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="match_parent"
        android:layout_height="400dp"
        android:scaleType="fitXY"
        android:layout_above="@id/btnAction"/>

    <TextView
        android:id="@+id/Preview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginStart="10dp"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="10dp"
        android:text="No Barcode Detected"
        android:textSize="18sp"/>

    <Button
        android:id="@+id/btnAction"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="10dp"
        android:layout_marginEnd="10dp"
        android:layout_marginBottom="10dp"
        android:text="No Barcode Detected" />

    <TextView
        android:id="@+id/switchbuttonadd"
        android:layout_width="wrap_content"
        android:layout_height="24dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:layout_marginEnd="10dp"
        android:layout_marginTop="10dp"
        android:text="Add"
        android:textSize="18sp"/>

    <androidx.appcompat.widget.SwitchCompat
        android:id="@+id/addremoveswitch"
        android:gravity="center"
        android:layout_width="wrap_content"
        android:layout_height="24dp"
        android:textAlignment="center"
        android:layout_toLeftOf="@id/switchbuttonadd"
        android:layout_marginTop="10dp"
        android:checked="true"
        android:layout_marginEnd="10dp" />

    <TextView
        android:id="@+id/switchbuttonremove"
        android:layout_width="wrap_content"
        android:layout_height="24dp"
        android:layout_toStartOf="@id/addremoveswitch"
        android:layout_alignParentTop="true"
        android:layout_marginTop="10dp"
        android:text="Remove"
        android:textSize="18sp"/>

</RelativeLayout>