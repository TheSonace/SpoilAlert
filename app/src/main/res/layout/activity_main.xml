<?xml version="1.0" encoding="utf-8"?>
<ViewFlipper xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/myViewFlipper"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <RelativeLayout
        android:id="@+id/main"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/colorBgMain"
        tools:context=".MainActivity">

        <TextView
            android:id="@+id/introText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="64dp"
            android:layout_centerHorizontal="true"
            android:text="@string/intro_text"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textAlignment="center"
            />


        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="32dp"
            android:layout_marginBottom="48dp"
            android:orientation="horizontal"
            />


        <ImageView
            android:id="@+id/introButton"
            android:layout_width="60dp"
            android:layout_height="match_parent"
            android:src="@drawable/double_arrow_right"
            android:layout_toLeftOf="@id/simpleSlidingDrawer"
            android:layout_alignTop="@id/simpleSlidingDrawer"
            android:layout_alignBottom="@id/simpleSlidingDrawer"
            app:iconGravity="textEnd"
            />


        <SlidingDrawer
            android:id="@+id/simpleSlidingDrawer"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:layout_alignParentBottom="true"
            android:layout_alignParentEnd="true"
            android:layout_marginBottom="250dp"
            android:orientation="horizontal"
            android:handle="@+id/handle"
            android:content="@+id/menuContent"
            >

            <Button
                android:id="@+id/handle"
                android:layout_width="10dp"
                android:layout_height="match_parent"
                android:gravity="end"
                android:textColor="#fff"
                android:background="@drawable/rounded_edge_slide_menu"
                />

            <LinearLayout
                android:id="@+id/menuContent"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:orientation="vertical"
                android:background="@drawable/layout_border"
                >

                <com.google.android.material.floatingactionbutton.FloatingActionButton
                    android:id="@+id/startscanbutton"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:clickable="true"
                    android:src="@android:drawable/ic_menu_add"
                    />

            </LinearLayout>

        </SlidingDrawer>



    </RelativeLayout>

    <include
        layout="@layout/activity_main_product_info"
        android:id="@+id/flipper_media" />

</ViewFlipper>